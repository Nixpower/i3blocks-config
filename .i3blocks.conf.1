# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/local/libexec/i3blocks/$BLOCK_NAME
markup=none
full_text=⎝
align=center
#color=#666666
color=#FF000D
separator=false
separator_block_width=7


# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
#[volume]
#label=VOL
#label=♪
#instance=Master
#instance=PCM
#interval=once
#signal=10

# Memory usage
#
# The type defaults to "mem" if the instance is not specified.

#[usb]
#command=/home/codingman/GitHub/i3blocks-usb/usb.py
#markup=pango
#signal=1
#interval=10

[separator]

[memory]
label=RAM:
color=#009933
separator=false
interval=30

[memory]
label=Swap:
color=#9999FF
instance=swap
separator=false
interval=30

# Disk usage
#
# The directory defaults to $HOME if the instance is not specified.
# The script may be called with a optional argument to set the alert
# (defaults to 10 for 10%).
[disk]
label=Disk:
color=#996600
instance=/
interval=30

[separator]

# Network interface monitoring
#
# If the instance is not specified, use the interface used for default route.
# The address can be forced to IPv4 or IPv6 with -4 or -6 switches.
[iface]
instance=eno1
color=#00FF00
interval=10
separator=false

[External IP]
command=curl icanhazip.com
color=#00FF00
interval=6000
separator=false

#[wifi]
#instance=wlp3s0
#interval=10
#separator=false

[bandwidth]
instance=eno1
color=#6600CC
interval=5

[separator]

# CPU usage
#
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
[cpu_usage]
label=CPU:
interval=10
color=#FFFF00
#min_width=CPU: 100.00%
separator=false

[load_average]
interval=10
separator=false
color=#7FC269
# Support multiple chips, though lm-sensors.
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.

[separator]

[temp0]
command=sensors coretemp-isa-0000 | awk '/Core 0/ { print $1 " " $2 " " $3 }'
interval=10
separator=false
color=#e8a31b

[separator]

[temp1]
command=sensors coretemp-isa-0000 | awk '/Core 1/ { print $1 " " $2 " " $3 }'
interval=10
separator=false
color=#e8a31b

[separator]

[temp2]
command=sensors coretemp-isa-0000 | awk '/Core 2/ { print $1 " " $2 " " $3 }'
interval=10
separator=false
color=#e8a31b

[separator]

[temp3]
command=sensors coretemp-isa-0000 | awk '/Core 3/  { print $1 " " $2 " " $3 }'
interval=10
separator=false
color=#e8a31b

# Generic media player support
#
# This displays "ARTIST - SONG" if a music is playing.
# Supported players are: spotify, vlc, audacious, xmms2, mplayer, and others.
#[mediaplayer]
#instance=spotify
#interval=5
#signal=10

# OpenVPN support
#
# Support multiple VPN, with colors.
#[openvpn]
#interval=20

# Temperature
#
# Key indicators
#
# Add the following bindings to i3 config file:
#
# bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
# bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks
#[keyindicator]
#instance=CAPS
#interval=once
#signal=11

#[keyindicator]
#instance=NUM
#interval=once
#signal=11
